node_modules/
dist/
.playwright-mcp/
*.tsbuildinfo
vite.config.js
vite.config.d.ts
courseforge-*.png
courseforge-*.imscc
.claude/settings.local.json
.DS_Store

# Local Netlify folder
.netlify

# Local env files with secrets (keep .env.example tracked as the template)
.env
.env.local
.env.*.local

# Keys and certificates (never commit)
*.pem
*.key
id_rsa

# Local screenshot / capture output (regenerable, never build inputs)
output/
artifacts/**/*.png
artifacts/**/*.jpg
